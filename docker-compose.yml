version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - BACKEND_URL=http://backend:8080
      - DATABASE_URL=${DATABASE_URL:-postgresql://prooftamil:localdev123@db:5432/prooftamil_local}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_GENAI_API_KEY=${GOOGLE_GENAI_API_KEY}
      - SESSION_SECRET=${SESSION_SECRET:-dev-secret-key}
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - prooftamil
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://prooftamil:localdev123@db:5432/prooftamil_local}
      - PORT=8080
      - GIN_MODE=release
      - GOOGLE_OAUTH_REDIRECT_DOMAIN=${GOOGLE_OAUTH_REDIRECT_DOMAIN:-https://prooftamil.com}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GOOGLE_GENAI_API_KEY=${GOOGLE_GENAI_API_KEY}
      - JWT_SECRET=${JWT_SECRET:-dev-jwt-secret}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - ADVANCED_SUGGEST_URL=http://suggest-service:8080
      - USE_ADVANCED_SUGGEST=${USE_ADVANCED_SUGGEST:-false}
    depends_on:
      db:
        condition: service_healthy
      suggest-service:
        condition: service_healthy
    networks:
      - prooftamil
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  suggest-service:
    build:
      context: ./backend/services/suggest-service
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://prooftamil:localdev123@db:5432/prooftamil_local}
      - PORT=8080
      - SUGGEST_TOP_K=50000
      - ENABLE_LLM_SUGGEST=false
      - NODE_ENV=production
    depends_on:
      db:
        condition: service_healthy
    networks:
      - prooftamil
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=prooftamil
      - POSTGRES_PASSWORD=localdev123
      - POSTGRES_DB=prooftamil_local
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prooftamil"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - prooftamil
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  prooftamil:
    driver: bridge
