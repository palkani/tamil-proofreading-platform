# Tamil Handwriting OCR - ProofTamil integration
# Gemini vision pipeline: preprocess -> segment -> 2-pass OCR -> correct -> confidence.

# Web
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Image processing (required)
opencv-python-headless>=4.8.0
numpy>=1.24.0
pydantic>=2.0.0
Pillow>=10.0.0

# Vision OCR + correction (required for the Gemini pipeline)
google-generativeai>=0.8.0

# Optional legacy fallback (Tesseract) — kept for a fast/offline mode.
# Install system tesseract-ocr + tesseract-ocr-tam to use it.
pytesseract>=0.3.10

# Optional: a future fine-tuned handwriting model (plan §9)
# torch>=2.0.0
